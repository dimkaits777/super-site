// ─────────────────────────────────────────────────────────────────────────
//  3D-МОДЕЛІ  —  ЄДИНЕ МІСЦЕ ДЛЯ ЗАМІНИ
//
//  Як підключити власну модель:
//   1. Покладіть файл .glb у папку  public/models/  (бажано з не-описовою
//      назвою, напр. 7f3a9b21.glb). Формат OBJ/FBX спершу конвертуйте у .glb.
//   2. Пропишіть його тут у відповідному ключі як обʼєкт:
//          { url: '/models/7f3a9b21.glb', rotation: [0, 0, 0], fit: 2 }
//   3. Якщо модель лежить на боці — це OBJ/Max експорт (Z-up). Поставте
//          rotation: [-Math.PI / 2, 0, 0]
//      Якщо догори дриґом — додайте Math.PI до потрібної осі.
//
//  Масштаб і центрування рахуються автоматично (FittedModel): модель завжди
//  стане на «підлогу», по центру, висотою ≈ `fit` юнітів. Тобто вручну треба
//  підбирати лише поворот (за потреби).
//
//  null  → використовується вбудована процедурна модель (запасний варіант).
//  Якщо файл не завантажиться — компонент автоматично відкотиться на процедурну.
//  (Згенерована запасна модель торта лежить у public/models/cba3316d.glb —
//   її перезаписує генератор scripts/generate-models.mjs, не чіпайте цю назву.)
// ─────────────────────────────────────────────────────────────────────────

const env = import.meta.env;

export const MODELS = {
  // СЦЕНА 2 — головний торт «Кокос» (реальна модель, конвертована з OBJ).
  cake: {
    url: env.VITE_CAKE_MODEL_URL || '/models/cca1fd05.glb',
    rotation: [-Math.PI / 2, 0, 0], // OBJ/Max експорт Z-up → ставимо вертикально
    fit: 2.0,
  },

  // СЦЕНА 3 — картки меню. Ключі збігаються з `id` у src/data/menu.js.
  // Приклад: napoleon: { url: '/models/abc12345.glb', rotation: [-Math.PI / 2, 0, 0], fit: 1.3 },
  'cake-kokos': null,
  napoleon: null,
  cheesecake: null,
  cappuccino: null,
  latte: null,
  'flat-white': null,
};
